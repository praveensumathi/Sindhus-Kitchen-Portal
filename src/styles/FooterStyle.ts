import { makeStyles } from "@mui/styles";

export const useCommonGridLayoutStyle = makeStyles(() => ({
  commonGridStyle: {
    textAlign: "center",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
}));
