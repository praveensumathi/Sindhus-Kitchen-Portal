import { makeStyles } from "@mui/styles";

export const useCommonGridStyle = makeStyles(() => ({
  commonGridStyle: {
    textAlign: "center",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
}));
